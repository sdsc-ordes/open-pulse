"""Open Pulse Hub — control-plane dashboard.

The FastAPI app is exposed as :data:`open_pulse.gui.hub.main.app`. Launch via
the CLI: ``open-pulse gui hub serve`` (requires the ``hub`` extra:
``pip install open-pulse-science[hub]``).
"""
