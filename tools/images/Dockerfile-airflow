FROM python:3.11-slim

WORKDIR /app

RUN pip install --no-cache-dir requests

# Placeholder: no airflow source exists yet.
# Build with context=project-root once src/airflow/ is added.
# COPY src/airflow/airflow_inference.py .

ENTRYPOINT ["python", "airflow_inference.py"]
