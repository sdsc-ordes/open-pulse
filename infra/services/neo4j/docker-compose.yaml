services:
  neo4j:
    image: neo4j:2026.04.0
    container_name: neo4j-open-pulse
    restart: unless-stopped

    ports:
      - "${NEO4J_HTTP_PORT:-7474}:7474"
      - "${NEO4J_BOLT_PORT:-7687}:7687"

    environment:
      NEO4J_AUTH: ${NEO4J_AUTH:-none}

      NEO4J_PLUGINS: ${NEO4J_PLUGINS:-'["graph-data-science","apoc"]'}

      # enable APOC unrestricted procedures
      NEO4J_dbms_security_procedures_unrestricted: ${NEO4J_UNRESTRICTED:-apoc.*}
      NEO4J_dbms_security_procedures_allowlist: ${NEO4J_ALLOWLIST:-apoc.*}

      NEO4J_ACCEPT_LICENSE_AGREEMENT: "yes"

    volumes:
      - ${NEO4J_DIR:-./data/neo4j}/logs:/logs
      - ${NEO4J_DIR:-./data/neo4j}/config:/config
      - ${NEO4J_DIR:-./data/neo4j}/data:/data
      - ${NEO4J_DIR:-./data/neo4j}/plugins:/plugins
